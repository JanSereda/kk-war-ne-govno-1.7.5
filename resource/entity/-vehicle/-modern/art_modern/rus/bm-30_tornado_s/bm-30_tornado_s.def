{Entity
	(include "bm-30.inc")
	{extender "inventory"
		{box
		    {item "ammo tornado_s 9m542 fg" 12}	
		}
	}
	;(mod not "mp"
	{Weaponry
		{place "gun"
			{weapon "tornado_s_launcher weapon" filling "ammo tornado_s 9m542 fg" 12}
			{gunner "gunner"}
			{charger "commander1"}
			{foresight "foresight"}
			{SequentialAim 0}		;наведение одновременно по курсу и дальности.
			{shooter "shooter"}
			{barrels 12}
		}
	}
	;)
	;(mod "mp"
	;{Weaponry
	;    {place "gun1"
	;		{weapon "spot" filled}
	;		;{basic}
	;	}
	;	{place "gun"
	;		{weapon "tornado_s_launcher weapon" filling "9m546" 12}
	;		{gunner "gunner"}
	;		{charger "commander1"}
	;		{foresight "foresight"}
	;		{SequentialAim 0}		;наведение одновременно по курсу и дальности.
	;		{shooter "shooter"}
	;		{barrels 12}
	;		{basic}
	;	}
	;}
	;)
	{Extension "bm-30_tornado_s.mdl"}
	("missile_aimpoint_trucksize")
	("CW_turr_speed" deg_per_s(6))	;это перебросочная, ее хватает
	;("CW_gun_rot_speed" deg_per_s(2.2))	;увы, очень медленно 
	("CW_gun_rot_speed" deg_per_s(6))	;поэтому тоже ставим 6
}
